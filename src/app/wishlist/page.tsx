import WishlistClient from '@/components/new-ui/WishlistClient';

export const metadata = {
  title: 'My Curated Wishlist | Encender',
  description: 'Items you have saved for your special moments and gifting.',
};

export default function WishlistPage() {
  return <WishlistClient />;
}
